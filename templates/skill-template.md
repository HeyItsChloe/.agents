# Skill Title

One sentence: what this skill does, when to use it, and what it does NOT do.

## Prerequisites

```bash
# Any required env vars or tools
[ -n "$GITHUB_TOKEN" ] && echo "set" || echo "missing"
```

## Quick Commands

```bash
# The most common invocation
command --flag value
```

## Step-by-Step

### Step 1 — [Action]

```bash
# commands
```

Expected output:
```
example output
```

### Step 2 — [Next action]

### Step N — Verify

```bash
# How to confirm the skill ran successfully
```

## Environment Requirements

| Requirement | Version | Notes |
|-------------|---------|-------|
| Tool name | >= X.Y | Why it's needed |

## Troubleshooting

**Problem:** [Common failure mode]
**Fix:** [How to resolve it]

**Problem:** [Another failure mode]
**Fix:** [How to resolve it]
